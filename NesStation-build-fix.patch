diff --git a/app/src/main/java/com/nesstation/app/ui/emulator/EmulatorScreen.kt b/app/src/main/java/com/nesstation/app/ui/emulator/EmulatorScreen.kt
index 378d0688..f6c94d5c 100644
--- a/app/src/main/java/com/nesstation/app/ui/emulator/EmulatorScreen.kt
+++ b/app/src/main/java/com/nesstation/app/ui/emulator/EmulatorScreen.kt
@@ -10571,11 +10571,12 @@ private fun SettingsPanel(
                     }
                 }
             }
+
+            // DC (Dreamcast/NAOMI) —— Flycast 独立核心，平台专属设置见 FlycastCoreSettings.kt
+            // （ImGui 菜单 + 独立 Compose 设置面板）。此处仅满足 Kotlin when 的 exhaustive 约束；
+            // EmulatorScreen 入口已对 DC 提前 return，运行时不会走到这里。
+            GamePlatform.DC -> { /* no-op: 见 FlycastCoreSettings.kt */ }
         }
-        // DC (Dreamcast/NAOMI) —— Flycast 独立核心，平台专属设置见 FlycastCoreSettings.kt
-        // （ImGui 菜单 + 独立 Compose 设置面板）。此处仅满足 Kotlin when 的 exhaustive 约束；
-        // EmulatorScreen 入口已对 DC 提前 return，运行时不会走到这里。
-        GamePlatform.DC -> { /* no-op: 见 FlycastCoreSettings.kt */ }
 
         Spacer(Modifier.size(8.dp))
         Text("修改后即时生效。设置与主界面设置同步。", color = Color(0xFF8899AA), fontSize = 11.sp)
